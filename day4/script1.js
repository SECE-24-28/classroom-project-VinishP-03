function addition()
{
    let a = document.getElementById('x').value;
    let b = document.getElementById('y').value;
    let z = parseInt(a) + parseInt(b);

    alert(z);
}

function multiplication()
{
    let a = document.getElementById('x').value;
    let b = document.getElementById('y').value;
    let z = parseInt(a) * parseInt(b);

    alert(z);
}

function subtraction()
{
    let a = document.getElementById('x').value;
    let b = document.getElementById('y').value;
    let z = parseInt(a) - parseInt(b);

    alert(z);
}

function division()
{
    let a = document.getElementById('x').value;
    let b = document.getElementById('y').value;
    let z = parseInt(a) / parseInt(b);

    alert(z);
}